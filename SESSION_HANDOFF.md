# ملف تسليم الجلسة — GiveChain Mobile

**لماذا هذا الملف:** إن تابع هذا المشروع أداة/جلسة أخرى (Claude أو أي مساعد آخر)، هذا الملف يلخّص أين توقفنا، ما الذي ننتظره من الباك اند، وما الذي يجب فعله عند وصول كل إجابة — دون الحاجة لإعادة قراءة المحادثة كاملة.

**تاريخ آخر تحديث:** 2026-08-24

---

## 1. الوضع الحالي بجملة واحدة

التطبيق نُفّذ ضد عقد `API_MOBILE.md` (الأحدث) بالكامل، مع إصلاح عدة أخطاء حرجة اكتُشفت أثناء المراجعة. **نحن الآن في وضع انتظار** — أرسلنا سؤالين للباك اند ولم يصل الرد بعد. لا تبدأ عملاً جديداً غير مرتبط بهذين الملفين قبل التحقق من وصول رد.

---

## 2. ما ننتظره من الباك اند — تحقق من هذا أولاً

### أ. `BACKEND_QUESTIONS_R2.md` (9 أسئلة، أُرسل، بانتظار الرد)

الأهم من بينها بالترتيب:

1. **الحساب البنكي/IBAN** — غير موجود في أي API؛ التطبيق يطلب "حوّل ثم أرفق إثبات" دون أن يعرف المستخدم إلى أين يحوّل. يحتاج قراراً من الباك اند (حقول على `CharityResponse`، أو endpoint موحّد، أو hardcode مؤقت).
2. **`GET /api/mobile/donations/{id}`** غير موجود — الحل المؤقت الحالي يمسح حتى 20 صفحة × 50 عنصر لإيجاد تبرع واحد. طلب رسمي مُرسَل.
3. **بنية `CaseNeed`** غير موثّقة رغم أن `caseNeedId` إلزامي لكل تبرع لحالة — تناقض حقيقي في وثيقتهم نفسها.
4. **`EntityId`/`EntityName`** لحالة `TargetType=Charity(3)` — غير معرّف في الوثيقة.
5. صيغ الحقول الديناميكية (`Boolean`/`Date`/`MultiOption`/`Attachment`) — نستخدم حالياً أعرافاً افتراضية معقولة (موثّقة في كود `DynamicAnswerField`) لحين التأكيد.
6. `PersonInfoFieldResponse` ينقصه حقل `Options` رغم دعم النوعين `Option`/`MultiOption`.
7. **`reset-password`: `token` أم `code`؟** — أرسلنا الحقلين معاً كحل مؤقت آمن (انظر §4 أدناه).
8. متى يُفعَّل `[Authorize]`؟ وهل تبقى شاشات التصفح العامة متاحة بلا توكن بعدها؟
9. تأكيد حقول `MediaFileResponse` وقيم `mediaType`.

### ب. `BACKEND_REQUEST_HELP_ENDPOINT.md` (طلب ميزة جديدة، أُرسل، بانتظار الرد)

قسم "هل تحتاج إلى مساعدة؟" في الصفحة الرئيسية **وهمي بالكامل حالياً** — لا يوجد أي endpoint له. الاستمارة تجمع بيانات حقيقية من المستخدم (بما فيها ملفات) ثم **ترميها فوراً** بدل إرسالها، وتعرض شاشة "نجاح" مضلِّلة برقم طلب مُولَّد محلياً.

**عند وصول الرد:**
- إن أعطوا endpoint → اربط `RequestHelpScreen` به (احذف `Future.delayed` في [request_help_screen.dart:159-160](lib/features/help_request/ui/request_help_screen.dart#L159-L160))، أنشئ `HelpRequestRepository`/`HelpRequestModels` بنفس نمط `complaint_repository.dart`.
- إن تأخر الرد لفترة طويلة والمستخدم يريد حلاً مؤقتاً → استبدل شاشة "النجاح" الوهمية بتنبيه صادق "هذه الميزة قيد التطوير" (قرار معلّق، لم يُنفَّذ بعد — كان آخر سؤال طرحته على المستخدم قبل هذا الملف).

---

## 3. ما تم إنجازه فعلياً هذه الجلسة (لا تُعِد عمله)

### إصلاحات حرجة (HIGH)
- **`ResetPasswordRequest`** يرسل الآن `token` و`code` معاً بنفس القيمة — [auth_models.dart:181-186](lib/features/auth/data/auth_models.dart#L181-L186) — لأن الوثيقتين تتعارضان ولا يمكن الجزم بأيهما صحيح.
- **`itemDescription`** أُضيف إلى `DonationResponse` ويُعرض الآن في شاشة تفاصيل التبرع — كان يُرفع من المستخدم ولا يُعرض له أبداً.
- **`PersonInfoAnswer.attachments`** كان يُحلَّل بشكل خاطئ (`whereType<Map>()` على قائمة نصوص = فارغة دائماً) — أُصلح، وأُضيف عرض المرفقات في `info_answers_screen.dart`.
- **`DynamicAnswerField`** ([lib/core/widgets/dynamic_answer_field.dart](lib/core/widgets/dynamic_answer_field.dart)) — ودجت موحّد لأنواع الحقول الديناميكية السبعة (Text/Numeric/Boolean/Date/Attachment/Option/MultiOption)، مطبّق في `signup_screen.dart`، `benefit_screens.dart`، `info_answers_screen.dart`.
- **`targetId`** في `DonationRecord` كان يُشتق من حقول غير موجودة في العقد (`campaignId`/`caseId`) — أُصلح ليستخدم `entityId` الموثّق.
- **`targetName`/`targetType`** أُضيف لهما فرع `Charity(3)` بدل الافتراض الخاطئ لـ"حملة".

### ميزة كاملة نُفّذت جولة سابقة (من قبل هذه الجلسة تحديداً لكن ضمن نفس المهمة الكبرى)
- **التبرع المباشر للجمعية** (`TargetType=Charity=3`) — مُنفَّذ بالكامل في طبقة البيانات (`donation_models.dart`, `donation_repository.dart`, `api_paths.dart`) وله واجهة (`_CharityDonateBar` في `charity_detail_screen.dart`). **لكن راجع §5 — الوصول إليه من التنقّل الرئيسي ضعيف جداً.**
- حذف `confirm-payment` بالكامل (endpoint غير موجود في العقد الجديد).
- إجبار اختيار `CaseNeed` قبل التبرع لحالة.

### اختبارات
كل التعديلات أعلاه لها اختبارات تراجع (`test/models_test.dart`, `test/donation_history_test.dart`, `test/profile_models_test.dart`). آخر تشغيل: **36/36 ناجح**، `flutter analyze` نظيف، أداتا `tools/ui_route_audit.py` و`tools/api_ui_coverage.py` تمرّان.

---

## 4. فجوات معروفة في التنقّل — لم تُصلَح بعد، بانتظار قرار المستخدم

هذه ليست أخطاء برمجية بل **قرارات تصميم UX** مؤجَّلة عمداً حتى يختار المستخدم الحل:

1. **التبرع المباشر للجمعية غير مكتشَف عملياً.** تبويب "تبرع" السفلي (`quick_donate_screen.dart`) يعرض حملات/حالات فقط، لا خيار "جمعية". بطاقة الجمعية في `/charities` بلا زر تبرع سريع. المسار الوحيد: الرئيسية → قسم "جمعيات موثوقة" → تفاصيل الجمعية → التمرير لأسفل لرؤية الشريط الثابت.
2. **شاشة المراكز (`/centers/map`) بلا مدخل مستقل.** الكود يعمل، لكنه غير مرتبط بأي تبويب/قسم/رابط في الرئيسية أو الملف الشخصي — الوصول الوحيد من داخل نموذج التبرع نفسه.

**لم يُطلب مني إصلاح هاتين النقطتين بعد** — فقط عُرضتا على المستخدم. إن استؤنفت الجلسة، اسأل المستخدم أولاً: هل يريد إضافة خيار "جمعية" لتبويب التبرع السريع؟ زر تبرع على بطاقة الجمعية؟ قسم "أقرب مركز" في الرئيسية؟ — هذه خيارات تصميم متعددة الاحتمالات ولا ينبغي تنفيذ أحدها من تلقاء نفسك دون تفضيل صريح.

---

## 5. بنود متوسطة الأولوية من تدقيق شامل سابق — لم تُطبَّق

تدقيق شامل (وكيل خلفي) لكل حقول الـDTOs غير المستخدمة كشف بنوداً متوسطة الأولوية لم تُنفَّذ بعد (ليست عاجلة، لكن مفيدة):

- فلاتر `GET /donations` الـ13 غير مستخدمة في الواجهة (Search/FromDate/ToDate/MinAmount/MaxAmount/…) — `findInHistory` في `donation_repository.dart` يمسح حتى 1000 سجل بدل استخدام فلتر مباشر.
- `IsStatusLocked`/`StatusLockNote`/`CaseNumber`/`CityId` (على الحالات) — موثّقة في العقد وغير مُحلَّلة في `CatalogItem`.
- `mediaType`/`isPrimary` تُفقَد في مسار التبرعات والشكاوى (تُحفظ فقط في الكاتالوج) — يسبب محاولة عرض PDF كصورة أحياناً.
- تعديل صورة الملف الشخصي غير ممكن (الـAPI الآن يدعمها حسب `BACKEND_ANSWERS.md` لكن الواجهة لم تُحدَّث بعد لاستخدام `multipart` بدل JSON).
- `TrustImageUrl` من الباك اند غير معروض (شارة الثقة محلية hardcoded حالياً).

هذه البنود **لم يطلبها المستخدم صراحة** بعد — اذكرها له إن سأل "ما تبقى؟" لكن لا تنفّذها من تلقاء نفسك.

---

## 6. خريطة الملفات المرجعية في جذر المشروع

| الملف | الغرض |
|---|---|
| `API_MOBILE.md` | العقد الرسمي الحالي من الباك اند (المرجع الأساسي) |
| `BACKEND_ANSWERS.md` | إجابات الباك اند على الجولة الأولى من الأسئلة |
| `BACKEND_ANSWERS_R2.md` | نسخة خام من إجابات إضافية (استُخرجت يدوياً بسبب تلف الترميز) |
| `BACKEND_QUESTIONS_R2.md` | أسئلة الجولة الثانية — **بانتظار الرد** |
| `BACKEND_REQUEST_HELP_ENDPOINT.md` | طلب endpoint لميزة "طلب مساعدة" — **بانتظار الرد** |
| `API_UI_COVERAGE_REPORT_AR.md` | تقرير تغطية API↔واجهات، يُولَّد تلقائياً بواسطة `tools/api_ui_coverage.py` |
| `GiveChain_Mobile_Guide.md` | دليل أقدم — بعض فقراته باتت قديمة (مثال: `code` بدل `token`)، **لا تعتمد عليه وحده عند التعارض مع `API_MOBILE.md`** |
| `mobile-donations.md` / `MOBILE_APP_GUIDE.md` | دليلان أقدم لتدفق التبرعات ومراكز الاستلام — تحقّقنا: **التطبيق يطابقهما فعلياً** (حالات التبرع التسع، proof/dropped-at-center، تقييد المراكز بالنوع) هذا التطابق كان موجوداً من قبل هذه الجلسة، وليس ناتجاً عن عملي |
| `SESSION_HANDOFF.md` | هذا الملف |

---

## 7. عند استئناف العمل — قائمة تحقق سريعة

1. اسأل المستخدم: هل وصل رد على `BACKEND_QUESTIONS_R2.md` أو `BACKEND_REQUEST_HELP_ENDPOINT.md`؟
2. إن وصل رد جزئي أو كامل: اقرأه، حدّث الكود بنداً بنداً، احذف أي حل مؤقت لم يعد ضرورياً (مثال: إن أكّدوا `token` فقط، احذف الحقل الزائد `code` من `ResetPasswordRequest.toJson()` وحدّث الاختبار المرتبط في `test/models_test.dart`).
3. بعد أي تعديل: شغّل `flutter analyze` و`flutter test` و`py tools/ui_route_audit.py` و`py tools/api_ui_coverage.py` — يجب أن تبقى كلها نظيفة.
4. لا تنسَ تحديث `BACKEND_QUESTIONS_R2.md`/`BACKEND_REQUEST_HELP_ENDPOINT.md` بعلامة ✅ أمام كل بند أُجيب عنه وطُبِّق، على غرار ما فعلناه في مطلع كل ملف أسئلة.
