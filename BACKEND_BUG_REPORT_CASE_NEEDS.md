# 🔴 عاجل: التبرع لكل الحالات (Cases) معطّل بالكامل — `CaseNeed` مطلوب إلزامياً لكنه غير متاح عبر أي API

**التاريخ:** 2026-08-30
**المكوّن:** `CasesController` + `DonationTargetResolver`
**الخطورة:** حرجة — **100% من الحالات (14/14) لا يمكن التبرع لها إطلاقاً** عبر تطبيق الموبايل.
**الحالة:** ليست مشكلة في التطبيق ولا في الداشبورد — التطبيق يمنع الإرسال عمداً لأن الخادم سيرفضه.

---

## 1. ملخّص المشكلة في سطرين

الخادم **يشترط `CaseNeedId` إلزامياً** لكل تبرع لحالة ويرفض الطلب بدونه.
لكن **لا يوجد أي endpoint أو حقل يُرجع قائمة الاحتياجات (`CaseNeed`)** حتى يختار منها المتبرّع.

**النتيجة:** حلقة مغلقة — التطبيق مطالَب بإرسال قيمة لا توجد أي وسيلة للحصول عليها. لذلك التبرع لكل الحالات متوقف.

---

## 2. ما يراه المستخدم فعلياً

عند فتح شاشة التبرع لأي حالة، تظهر هذه الرسالة بدل نموذج التبرع:

> ⚠️ لا توجد احتياجات متاحة للتبرع في هذه الحالة حالياً، لذلك لا يمكن إتمام التبرع لها. جرّب حالة أو حملة أخرى، أو تبرّع مباشرة للجمعية.

**هذا ليس خطأً في التطبيق.** التطبيق يعرض الرسالة عن قصد لأنه لا يستطيع تكوين طلب صالح. البديل الوحيد هو ترك المتبرّع يملأ النموذج كاملاً ثم يفشل الإرسال بخطأ Validation من الخادم — وهي تجربة أسوأ.

**مهم:** الحالات **نشطة وغير معطّلة من الداشبورد** (`status: 0`, `closedAt: null`, `isStatusLocked: false`). المشكلة ليست في بيانات الحالة.

---

## 3. إثبات عملي من الخادم الحيّ (production)

تم اختبار `https://givechain.runasp.net` بتاريخ 2026-08-30.

### 3.1 استجابة تفاصيل الحالة لا تحتوي `needs`

```
GET https://givechain.runasp.net/api/mobile/cases/14d84b5d-71db-4271-8287-aa3191a3dbca
```

```json
{
  "message": null,
  "data": {
    "id": "14d84b5d-71db-4271-8287-aa3191a3dbca",
    "charityId": "1692f4d1-153f-41a7-bec5-0983370e3b66",
    "caseNumber": "",
    "categoryId": null, "category": null, "priority": 0,
    "cityId": null, "locationDetails": null,
    "title": "طفل يعاني من فرط النشاط ونقص الانتباه",
    "description": "طفل يعاني من فرط النشاط ونقص الانتباه أو ما يعرف ب ADHD يلزمه حوالي 250$ للأدوية اللازمة شهريا",
    "imageUrl": null,
    "status": 0,
    "publishedAt": "2026-08-28T18:44:45.8230783",
    "closedAt": null, "closedNotes": null,
    "createdBy": "d30310ba-5a2b-4429-b5ce-e3de522a8c09",
    "createdByUserName": null, "creator": null,
    "medias": [],
    "isStatusLocked": false,
    "statusLockNote": null
  },
  "errors": null,
  "isSuccess": true
}
```

☝️ **لا يوجد أي حقل اسمه `needs` أو `caseNeeds` أو ما شابه.**

### 3.2 المشكلة تشمل كل الحالات بلا استثناء

تم فحص **كل** الحالات الـ 14 الموجودة (`GET /api/mobile/cases?pageSize=14`)، ثم استدعاء endpoint التفاصيل لكل واحدة منها على حدة:

| النتيجة | العدد |
|---|---|
| `HTTP 200` (الحالة موجودة وتعمل) | 14 / 14 |
| تحتوي على أي حقل فيه كلمة `need` | **0 / 14** |

عدد مرّات ورود النص `need` في استجابة القائمة الكاملة (كل الـ 14 حالة): **صفر**.

هذا يؤكد أن المشكلة **بنيوية في الـ DTO** وليست بيانات ناقصة في حالة معيّنة.

### 3.3 لا يوجد endpoint بديل لجلب الاحتياجات

جُرّبت كل التسميات المحتملة، وكلها **404**:

| المسار | النتيجة |
|---|---|
| `GET /api/mobile/cases/{id}/needs` | `404` |
| `GET /api/mobile/cases/{id}/case-needs` | `404` |
| `GET /api/mobile/case-needs?caseId={id}` | `404` |
| `GET /api/mobile/caseneeds?caseId={id}` | `404` |
| `GET /api/mobile/needs?caseId={id}` | `404` |
| `GET /api/mobile/cases/{id}/details` | `404` |

---

## 4. لماذا يستحيل على التطبيق الالتفاف على المشكلة

هذا مقتطف من ردّكم السابق الموثّق في `BACKEND_ANSWERS.md` (سؤال 5) — من `DonationTargetResolver.ResolveCaseNeedAsync`:

```csharp
if (caseNeedId is null)
    return (null, "CaseNeedId is required for a case donation.");
```

وجاء في نفس الردّ حرفياً:

> "على شاشة التبرع لحالة، لازم تطبيق الموبايل يجبر المستخدم على اختيار احتياج محدد (`CaseNeed`) قبل إرسال الطلب — **لا يوجد Fallback تلقائي**."

كما أن `POST /api/mobile/cases/{id}/donate` — رغم أنه يملأ `CaseId` تلقائياً من المسار — **لا يستنتج `CaseNeedId`**، وهذا موثّق في `API_MOBILE.md`:

> "you still need to supply `CaseNeedId` yourself (required for case donations) since it isn't inferred from the route."

**الخلاصة:** لا يوجد fallback، ولا استنتاج تلقائي، ولا مصدر للقيمة. الطريق مسدود من الجهتين.

---

## 5. التناقض موثّق في وثيقتكم نفسها

وثيقة `API_MOBILE.md` تعترف بالفجوة صراحةً:

> "a case's donatable *needs* (`CaseNeed`, with `CaseNeedId` used when donating) **aren't exposed as a distinct field on `CaseResponse`** in this controller set — needs are referenced by id when creating a donation; **check with the backend team if a needs-listing endpoint is required**."

الجواب على هذا السؤال المعلّق هو: **نعم، مطلوب — وبشكل عاجل**، لأن بدونه ميزة التبرع للحالات معطّلة كلياً.

> 📌 ملاحظة: هذه النقطة سبق طرحها في `BACKEND_QUESTIONS_R2.md` (سؤال 3) ولم يصلنا ردّ بشأنها بعد. نعيد رفعها هنا لأنها انتقلت من "استفسار" إلى **عطل مؤكَّد في production**.

---

## 6. المطلوب — أحد الحلّين (نُفضّل الأول)

### ✅ الحل المفضّل: إضافة `Needs` إلى `CaseResponse`

بحيث يعود `GET /api/mobile/cases/{id}` (و`GET /api/mobile/cases` إن أمكن) بالشكل:

```json
{
  "id": "14d84b5d-...",
  "title": "طفل يعاني من فرط النشاط ونقص الانتباه",
  "needs": [
    {
      "id": "3f2a...-guid",
      "name": "أدوية شهرية",
      "type": 1,
      "unit": 0,
      "targetQuantity": 250,
      "fulfilledQuantity": 40,
      "isFulfilled": false
    }
  ]
}
```

**ميزة هذا الحل:** التطبيق يقرأ حالياً مصفوفة `needs` من نفس الاستجابة، لذا **سيعمل فوراً دون أي تعديل أو إصدار جديد** بمجرد نشركم للتغيير.

### 🔄 الحل البديل: endpoint مستقل

```
GET /api/mobile/cases/{id}/needs  →  CaseNeedResponse[]
```

مقبول أيضاً، لكنه يتطلّب تعديلاً وإصداراً جديداً من التطبيق.

---

## 7. الحقول المطلوبة في كل عنصر `CaseNeed`

| الحقل | النوع | إلزامي | لماذا نحتاجه |
|---|---|---|---|
| `id` | `Guid` | ✅ | هذه هي القيمة التي تُرسل كـ `CaseNeedId` — بدونها لا يتم التبرع |
| `name` / `title` | `string` | ✅ | لعرضه للمتبرّع في القائمة المنسدلة |
| `type` | `CaseNeedType` | ✅ | لمطابقة نوع التبرع (مال/مادة/خدمة) ومنع التركيبة الخاطئة قبل الإرسال |
| `unit` | `CaseNeedUnit?` | ⬜ | لعرض وحدة القياس للتبرع العيني |
| `targetQuantity` | `decimal?` | ⬜ | لعرض المطلوب |
| `fulfilledQuantity` | `decimal?` | ⬜ | لعرض نسبة الإنجاز وإخفاء المكتمل |
| `isFulfilled` / `status` | `bool` / `enum` | ⬜ | لإخفاء الاحتياجات المكتملة من القائمة |

### ❓ سؤال مهم مرتبط: قيم `CaseNeedType`

ذكرتم أن **نوع التبرع يجب أن يطابق `CaseNeedType`** وإلا رُفض الطلب. لكي نمنع المستخدم من اختيار تركيبة خاطئة **قبل** الإرسال، نحتاج القيم الرقمية الدقيقة:

- ما هي قيم `CaseNeedType` بالضبط؟
- هل تطابق ترقيم `DonationType` (`Money=1, Item=2, Service=3`) أم لها ترقيم مختلف؟

---

## 8. خطوات إعادة إنتاج المشكلة

1. افتح التطبيق ← قسم **الحالات**.
2. اختر أي حالة (مثلاً "عائلة نازحة تبحث عن مأوى").
3. اضغط **تبرّع**.
4. **المتوقع:** قائمة منسدلة لاختيار الاحتياج المراد تمويله، ثم إمكانية إتمام التبرع.
5. **الواقع:** تحذير أصفر "لا توجد احتياجات متاحة للتبرع"، وتعذّر إتمام التبرع.
6. يتكرر هذا مع **كل** حالة في التطبيق بلا استثناء.

**للتحقق السريع من طرفكم دون التطبيق:**

```
curl "https://givechain.runasp.net/api/mobile/cases/14d84b5d-71db-4271-8287-aa3191a3dbca"
```

لاحظوا غياب أي حقل "needs" في الاستجابة.

---

## 9. الأثر التجاري

- **ميزة التبرع للحالات الإنسانية معطّلة 100%** في التطبيق.
- الحملات (`Campaigns`) والتبرع المباشر للجمعيات **يعملان بشكل طبيعي** — المشكلة محصورة في الحالات.
- كل حالة تُنشئها الجمعيات من الداشبورد لن تستقبل أي تبرع حتى يُحلّ هذا.

---

## 10. المرجع في كود التطبيق

- قراءة الاحتياجات: `lib/features/donations/ui/donation_screen.dart` — الدالة `_caseNeeds()` (سطر 277)
- عرض رسالة التحذير: نفس الملف، سطر 313
- التحقق قبل الإرسال: نفس الملف، سطر 773

هذا المنطق **جاهز ويعمل** — ينتظر فقط أن تصله البيانات من الـ API.

---

## 📮 ملخص الطلب

| # | المطلوب | الأولوية |
|---|---|---|
| 1 | إضافة `Needs` إلى `CaseResponse` (أو endpoint `GET /cases/{id}/needs`) | 🔴 عاجل |
| 2 | تزويدنا بقيم enum `CaseNeedType` الرقمية | 🟠 مهم |
| 3 | تأكيد اسم الحقل وشكل العنصر النهائي لنطابقه في التطبيق | 🟡 متابعة |
