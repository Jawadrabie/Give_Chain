# تأكيد إعادة الاختبار — عطل التسجيل وتسجيل الدخول

**التاريخ:** 2026-08-24
**بخصوص:** `BACKEND_BUG_FIX_REPORT.md` (ردكم على `BACKEND_BUG_REPORT_REGISTER.md`)

شكراً على الفحص السريع والدقيق. أعدنا الاختبار مباشرة على `givechain.runasp.net` بنفس سيناريوهات التقرير الأصلي، وطبّقنا ما يلزم من جهتنا.

---

## 1 و 2 — تأكدنا: تم الإصلاح فعلياً على الخادم الحيّ

| السيناريو | النتيجة الآن |
|---|---|
| `register` بدون `NationalNumber` | ✅ ينجح، ويرجع `token` صالحاً |
| `login` بكلمة مرور خاطئة | ✅ `HTTP 400` + `isSuccess: false` + `"Invalid email or password."` |
| `register` ببريد مكرر | ✅ `HTTP 400` + `isSuccess: false` + `"An account with this email already exists."` |

تدفّق التسجيل وتسجيل الدخول يعمل بشكل كامل الآن من طرفكم. لم تتطلب هذه النقطتان أي تعديل في كود التطبيق — طبقة معالجة الأخطاء لدينا (`ApiFailure.fromPayload`) كانت مبنية أصلاً على توقّع `isSuccess: false` عند الفشل، وكانت مختبَرة بذلك مسبقاً؛ المشكلة كانت فقط أن الخادم لم يكن يُرجعها. شغّلنا مجموعة الاختبارات الكاملة (36 اختباراً) بعد التأكيد وكلها ناجحة.

## 3.1 — لا حاجة لتعديل كود من طرفنا؛ بانتظار تصحيح البيانات

مفهوم أنها بيانات في `PersonInfoFields` لا كود. لم نعدّل شيئاً؛ بانتظار تصحيح `fieldType` لحقل «تاريخ الميلاد» إلى `3` (Date) من لوحة الإدارة.

## 3.2 — قرارنا: لا نُخفي أي حقل ديناميكي من جهة التطبيق

راجعنا فكرة إخفاء «رقم الهوية»/«تاريخ الميلاد» عن شاشة التسجيل (كانت مطروحة كخيار مؤقت) وتراجعنا عنها. مهمة التطبيق أن يعرض كل ما ترجعه `info-fields` كما هو دون تخمين أو تصفية من جانبنا — أي فلترة بمطابقة اسم نصّي ستنكسر أول ما تُغيّرون تسمية حقل من لوحة الإدارة، وقد تُخفي حقلاً مطلوباً بصمت. شاشة التسجيل تعرض الآن كل الحقول الديناميكية كما هي، دون استثناء.

**القرار المتعلق بالتكرار (رقم الهوية/تاريخ الميلاد مع `NationalNumber`/`BirthOfDate`) يبقى مفتوحاً من جهتكم فقط**: إن رأيتم أنه تكرار غير مقصود، الحل عندكم — حذف الحقلين من `info-fields`، أو دمجهما مع الحقول الثابتة، أو تمييزهما بغرض مختلف واضح للمستخدم. لن نتخذ أي إجراء تصفية من جهة التطبيق.

## ملاحظة الـ JWT (`uid` لا `UserId`)

مفهوم أنه تصحيح توثيقي فقط في `API_MOBILE.md` ولا يحتاج تعديل كود. سنعتمد `uid` كمرجع من جهتنا في أي وثائق مستقبلية.

---

**الخلاصة:** لا عطل حاجب متبقٍ. الشيء الوحيد المفتوح فعلياً هو تصحيح بيانات `fieldType` (البند 3.1) من طرفكم — وهو تحسين جودة بيانات لا يحجب أي تدفّق.
