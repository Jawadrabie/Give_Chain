# تقرير عطل حاجب — إنشاء الحساب وتسجيل الدخول لا يعملان من التطبيق

**التاريخ:** 2026-08-24
**الخطورة:** حاجب (Blocker) — **لا يستطيع أي مستخدم جديد إنشاء حساب من التطبيق حالياً**
**البيئة المختبَرة:** `https://givechain.runasp.net` (اختبار مباشر بـ`curl`، خارج التطبيق)

> هذا التقرير ليس تخميناً من قراءة الكود: كل استجابة أدناه منسوخة حرفياً من نداء فعلي على الخادم الحيّ بتاريخ 2026-08-24.

---

## ملخص تنفيذي

عُطلان مستقلان يتراكبان فينتج عنهما فشل صامت ومُضلِّل:

| # | العطل | الأثر |
|---|---|---|
| **1** | `NationalNumber` **إلزامي فعلياً** رغم أن الوثيقة تقول إنه اختياري | كل محاولة تسجيل من التطبيق تُرفض — الحساب **لا يُنشأ إطلاقاً** |
| **2** | الخادم يرجع `isSuccess: true` **مع كل رسالة خطأ** وبحالة `HTTP 200` | التطبيق لا يستطيع اكتشاف الفشل، فيُخفي سبب الخطأ الحقيقي ويعرض رسالة خاطئة |

**نتيجة تراكبهما على المستخدم:** يملأ النموذج، يضغط «إنشاء الحساب»، فتظهر له:

> «تم إنشاء الحساب لكن الخادم لم يرجع رمز جلسة صالحًا.»

وهي رسالة **كاذبة تماماً**: الحساب لم يُنشأ، والخادم قال بوضوح `"National number is required."` لكن التطبيق لم يستطع رؤية ذلك بسبب العطل رقم 2. ثم يحاول المستخدم تسجيل الدخول بالحساب الذي ظنّ أنه أُنشئ فيُرفض — لأنه غير موجود أصلاً.

---

## العطل 1 — `NationalNumber` إلزامي رغم توثيقه كاختياري

### التوثيق الحالي
`API_MOBILE.md` سطر 48، جدول `MobileRegisterRequest`:

| Field | Type | Required | Notes |
|---|---|---|---|
| NationalNumber | `string?` | **no** | |

### السلوك الفعلي

**الطلب** (كل الحقول الإلزامية موثّقة موجودة، و`NationalNumber` محذوف كما تسمح الوثيقة):

```bash
curl -X POST "https://givechain.runasp.net/api/mobile/auth/register" \
  -F "FirstName=TestQA" -F "LastName=Probe" \
  -F "Username=qaprobe_gc01" -F "Email=qaprobe_gc01@example.com" \
  -F "Password=Test@12345" -F "Gender=0"
```

**الاستجابة:**
```json
HTTP/1.1 200 OK
{"message":"National number is required.","data":null,"errors":null,"isSuccess":true}
```

**إثبات مضاد** — نفس الطلب بالضبط مع إضافة `NationalNumber` وحده، **ينجح فوراً**:

```bash
curl -X POST "https://givechain.runasp.net/api/mobile/auth/register" \
  -F "FirstName=TestQA" -F "LastName=Probe" \
  -F "Username=qaprobe_gc02" -F "Email=qaprobe_gc02@example.com" \
  -F "Password=Test@12345" -F "Gender=0" \
  -F "NationalNumber=1234567890"
```
```json
{"message":null,"data":{"userId":"b921c650-…","token":"eyJhbGciOiJIUzI1NiIs…","email":"qaprobe_gc02@example.com","fullName":"TestQA Probe","userType":0,"userName":"qaprobe_gc02","userStatus":0,…},"errors":null,"isSuccess":true}
```

فالمتغيّر الوحيد بين الفشل والنجاح هو `NationalNumber`. **العقد الموثّق مخالف للتنفيذ.**

### المطلوب — اختاروا أحد الاتجاهين

- **(أ)** جعله اختيارياً فعلاً في `MobileRegisterRequest` (حذف شرط التحقق) — **وهو المفضّل لدينا**، لأن الرقم الوطني بيانات حساسة وطلبها كشرط لفتح حساب متبرّع يرفع نسبة الارتداد في التسجيل بشكل ملموس. يمكن طلبه لاحقاً فقط عند التقدّم لطلب منفعة (Benefit) حيث يكون مبرَّراً.
- **(ب)** إبقاؤه إلزامياً + **تحديث `API_MOBILE.md` إلى `Required: yes`** وإبلاغنا، فنجعله حقلاً مطلوباً بنجمة في التطبيق مع تحقق محلي.

نرجو تحديد الاختيار صراحةً، لأننا لا نستطيع اعتماد أيٍّ منهما من طرفنا دون تأكيدكم.

**سؤال ملحق:** إن بقي إلزامياً — ما قواعد التحقق؟ (طول ثابت؟ أرقام فقط؟ فريد لكل مستخدم؟) نحتاجها لنمنع الرفض قبل إرسال الطلب بدل أن يكتشفه المستخدم بعد الإرسال.

---

## العطل 2 — `isSuccess: true` مع كل استجابة فشل (الأخطر)

هذا **العطل الجذري**، وأثره يتجاوز شاشة التسجيل إلى **كل نداء في التطبيق**.

الغلاف (envelope) الموحّد يرجع دائماً `isSuccess: true` و`HTTP 200` **حتى في حالات الفشل الصريح**. ثلاثة أمثلة حقيقية:

| الحالة | الاستجابة الفعلية |
|---|---|
| كلمة مرور خاطئة | `{"message":"Invalid email or password.","data":null,"errors":null,"isSuccess":true}` |
| حقل إلزامي ناقص | `{"message":"National number is required.","data":null,"errors":null,"isSuccess":true}` |
| بريد مكرر | `{"message":"An account with this email already exists.","data":null,"errors":null,"isSuccess":true}` |

الحقل `isSuccess` هو **الإشارة الوحيدة المتاحة برمجياً** للتمييز بين النجاح والفشل (لا حالة HTTP مفيدة، و`errors` دائماً `null`). وبما أنه `true` دائماً، **فلا سبيل لأي عميل — تطبيقنا أو أي عميل آخر — أن يكتشف الفشل عبر العقد الموثّق.**

### كيف يُنتج هذا الرسالة المضلِّلة

طبقة الشبكة لدينا تكتشف أخطاء `HTTP 200` عبر `isSuccess == false` (وهو النمط القياسي لهذا الغلاف). ولأنه `true`:

1. التطبيق يعتبر الاستجابة **ناجحة**، ويتجاهل `"National number is required."` تماماً.
2. يحاول قراءة `data.token` — و`data` هي `null` — فيخرج التوكن فارغاً.
3. تنطلق الحماية الأخيرة لدينا فتعرض: «تم إنشاء الحساب لكن الخادم لم يرجع رمز جلسة صالحًا.»

فالرسالة الحقيقية موجودة في الاستجابة، لكن `isSuccess: true` يمنع التطبيق من الوصول إليها.

### المطلوب

1. **إرجاع `isSuccess: false` في كل استجابة فشل.** هذا وحده يُصلح المشكلة فوراً ويجعل التطبيق يعرض رسالتكم الحقيقية للمستخدم بدل الرسالة العامة.
2. **(مستحسن بشدة) إرجاع حالة HTTP صحيحة** بدل `200` في كل الحالات:
   - `400 Bad Request` — فشل تحقق (حقل ناقص/غير صالح)
   - `401 Unauthorized` — بيانات دخول خاطئة
   - `409 Conflict` — بريد/اسم مستخدم مكرر

   هذا يجعل السلوك صحيحاً حتى لو تغيّر شكل الغلاف مستقبلاً، ويتكامل مع معالجة `401` القائمة لدينا (تنظيف الجلسة تلقائياً). ويتقاطع هذا مع البند 6 في `BACKEND_CONTRACT_NOTES.md` («تفعيل `[Authorize]` وإرجاع 401 بدل 500»).
3. **(مستحسن) تعبئة `errors[]`** بأخطاء التحقق لكل حقل على حدة، لنُظهر الخطأ أسفل الحقل المعني مباشرة بدل رسالة عامة أعلى الشاشة.

---

## العطل 3 — تصنيف خاطئ لحقل «تاريخ الميلاد» في `info-fields` (أقل خطورة)

**الطلب:** `GET /api/mobile/auth/info-fields`

**الاستجابة الفعلية:**
```json
{"data":[
  {"id":"1231de82-…","fieldName":"رقم الهوية",  "fieldType":1,"userType":0,"isRequired":false},
  {"id":"b2c2d61e-…","fieldName":"تاريخ الميلاد","fieldType":1,"userType":0,"isRequired":false}
]}
```

مشكلتان هنا:

### 3.1 — `تاريخ الميلاد` مصنَّف `fieldType: 1` (Numeric) بدل `3` (Date)

التطبيق يبني الحقل الديناميكي حسب `fieldType` القادم منكم: `Date=3` يُنتج **منتقي تاريخ حقيقي** يضمن صيغة `yyyy-MM-dd` سليمة، بينما `Numeric=1` يُنتج **صندوق أرقام حراً بلا أي تحقق**.

والنتيجة عملياً: أدخل مستخدم اختبار القيمة `2004` وقُبلت كما هي، وستُخزَّن عندكم كتاريخ ميلاد بلا معنى. أي قيمة رقمية أخرى (`13`, `99999`) ستُقبل أيضاً.

**المطلوب:** تغيير `fieldType` لهذا الحقل إلى `3` (Date) في لوحة الإدارة. لا يلزم أي تغيير في التطبيق — سيلتقط التصنيف الجديد تلقائياً ويعرض منتقي تاريخ.

### 3.2 — الحقول الديناميكية تُكرِّر حقولاً ثابتة في نموذج التسجيل

`رقم الهوية` و`تاريخ الميلاد` موجودان **مرتين** في شاشة إنشاء الحساب:
- مرة كحقلين ثابتين من عقد `register` (`NationalNumber` / `BirthOfDate`)
- ومرة كحقلين ديناميكيين من `info-fields`

هذا التكرار **هو ما أوقع مستخدم الاختبار في العطل 1**: ملأ «رقم الهوية» الديناميكي ظاناً أنه المطلوب، وترك `الرقم الوطني` الثابت فارغاً — فرفض الخادم الطلب.

**المطلوب:** حذف هذين الحقلين من `info-fields` (فبياناتهما تُلتقط أصلاً في `register`)، أو تأكيد أنهما مقصودان لغرض مختلف فنُخفيهما من شاشة التسجيل ونُبقيهما في الملف الشخصي فقط.

---

## ملاحظة جانبية — اسم مُطالبة (claim) الـ JWT

فكّ ترميز التوكن الراجع يُظهر:
```json
{"uid":"b921c650-4a08-4883-92bf-056a830d8d47","exp":1790142285,"iss":"GiveChain","aud":"GiveChainUsers"}
```

بينما `API_MOBILE.md` سطر 14 يقول: *"`CurrentUserId` is read from the JWT's **`UserId`** claim"*.

الاسم الفعلي `uid` لا `UserId`. هذا لا يؤثر علينا (نمرّر التوكن كما هو)، لكنه قد يعني أن `CurrentUserId` سيقرأ `null` عند تفعيل `[Authorize]` فعلياً — نرفعه الآن تحسّباً لأن اكتشافه بعد التفعيل سيبدو عطلاً غامضاً في كل الـendpoints المحمية دفعة واحدة.

---

## خطوات إعادة إنتاج العطل من التطبيق

1. افتح «إنشاء حساب جديد».
2. املأ الحقول الإلزامية فقط (الاسم الأول، اسم العائلة، اسم المستخدم، البريد، كلمة المرور).
3. اترك **«الرقم الوطني (اختياري)»** فارغاً — كما تسمح الوثيقة صراحةً.
4. اضغط «إنشاء الحساب».
5. **المتوقّع:** إنشاء الحساب بنجاح (أو رسالة واضحة تحدد الحقل الناقص).
   **الفعلي:** «تم إنشاء الحساب لكن الخادم لم يرجع رمز جلسة صالحًا.» — والحساب لم يُنشأ.
6. جرّب تسجيل الدخول بنفس البيانات → «لم يرجع الخادم رمز جلسة صالحًا» (لأن الحساب غير موجود، والرسالة الحقيقية `Invalid email or password.` محجوبة بالعطل 2).

---

## الأولوية المقترحة

| الأولوية | الإصلاح | الجهد المتوقع |
|---|---|---|
| **1 — عاجل** | `isSuccess: false` في استجابات الفشل (العطل 2) | سطر واحد في الغلاف الموحّد؛ يُصلح إخفاء الأخطاء في **كل** التطبيق |
| **2 — عاجل** | حسم إلزامية `NationalNumber` (العطل 1) | حذف شرط تحقق، أو تحديث سطر واحد في الوثيقة |
| **3 — متوسط** | حالات HTTP صحيحة (400/401/409) | تحسين بنيوي |
| **4 — منخفض** | `fieldType` لتاريخ الميلاد + إزالة التكرار (العطل 3) | تعديل بيانات من لوحة الإدارة |

**إصلاح البندين 1 و2 وحدهما يُعيد تدفّق التسجيل للعمل بالكامل.**

---

## حسابات اختبار أنشأناها أثناء الفحص

للحذف من قاعدة البيانات عند الانتهاء:

- `qaprobe_gc02@example.com` (`userId: b921c650-4a08-4883-92bf-056a830d8d47`) — أُنشئ فعلياً
- `qaprobe_gc01@example.com` — لم يُنشأ (فشل بالعطل 1)

---

*أُعدّ هذا التقرير من فريق تطبيق الموبايل بتاريخ 2026-08-24، بعد اختبار مباشر على الخادم الحيّ. الملف محفوظ بترميز UTF-8 كما اتُّفق في `BACKEND_QUESTIONS_R2.md`.*
