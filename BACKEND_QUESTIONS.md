# أسئلة واستفسارات تقنية لمسؤول الباك اند (Backend Lead) — GiveChain Mobile

> ✅ **تم الرد على جميع الأسئلة أدناه بتاريخ 2026-08-23** — الإجابات الكاملة في [`BACKEND_ANSWERS.md`](BACKEND_ANSWERS.md).
> **ما زال مفتوحاً ويحتاج قراراً من الفريق:** السؤال 1 (بيانات الحساب البنكي/IBAN غير موجودة إطلاقاً في الباك اند)، والسؤال 3 (لا يوجد endpoint لتفاصيل تبرع واحد — الباك اند عرض إضافته عند الطلب).
> **نُفِّذ في التطبيق بناءً على الإجابات:** حذف `confirm-payment` بالكامل (سؤال 6)، وإجبار اختيار الاحتياج قبل التبرع لحالة (سؤال 5).

تحية طيبة، بناءً على مراجعة وتحليل وثائق الباك اند الخاصة بموبايل GiveChain (`mobile-donations.md` و `MOBILE_APP_GUIDE.md`) وتطوير واجهات تطبيق Flutter، يرجى التكرم بتوضيح النقاط التقنية التالية:

---

### 1. بيانات الحسابات البنكية للتحويل الخارجي (Bank Account Details)
- عند اختيار المتبرع لـ `PaymentMethod = BankTransfer (1)`، من أين يجلب التطبيق رقم الحساب البنكي ورمز IBAN الخاص بالجمعية/المنصة ليقوم بالتحويل المالي إليه؟ 
- هل يوجد Endpoint مخصص مثل `GET /api/mobile/bank-accounts` أو تُرجع البيانات ضمن تفاصيل الحملة/الحالة/الجمعية؟

---

### 2. اسم حقل الملف في Endpoint إشعار التحويل (`Proof Upload`)
- في وثيقة `MOBILE_APP_GUIDE.md` تم ذكر:
  `POST /api/mobile/donations/{id}/proof` مع ملاحظة `field: files`.
- هل اسم الحقل في الـ `multipart/form-data` هو `files` أم `attachments`؟ (للعلم: في إنشاء التبرع اسم الحقل هو `attachments`).

---

### 3. استعلام تفاصيل تبرع مالي مفرط (Single Donation Detail Endpoint)
- تُشير الوثيقة حالياً إلى جلب التبرعات عبر السجل المرقّم `GET /api/mobile/donations?page=...`.
- هل يوجد Endpoint مباشر لجلب تبرع واحد برقم معرفه مثل `GET /api/mobile/donations/{id}` لتسريع تحديث شاشة التفاصيل وتفادي البحث في صفحات السجل؟

---

### 4. معرفات المدن والدول في الـ Centers Endpoint
- في `GET /api/mobile/centers?cityId=&countryId=`، هل معلمات `cityId` و `countryId` تقبل قيم الـ GUID الناتجة من `GET /api/lookup/cities` و `GET /api/lookup/countries` بنفس التنسيق؟

---

### 5. التبرع للحالة بدون احتياج عبر الـ General Endpoint
- عند التبرع لحالة إنسانية عبر `POST /api/mobile/donations` بدون إرسال `caseNeedId`، هل يرفض الخادم الطلب بـ Validation Error أم يربطه تلقائيًا بأول احتياج مطابق لنوع التبرع؟

---

### 6. حالة Endpoint تأكيد التحويل (`confirm-payment`) — عاجل
- وثيقة `API_MOBILE.md` (النسخة المحدثة) لا تذكر إطلاقاً `POST /api/mobile/donations/{id}/confirm-payment`، بينما تطبيق الموبايل يستخدمه فعليًا حاليًا (زر "تأكيد التحويل" في شاشتي نتيجة التبرع وسجل التبرعات).
- هل هذا الـ Endpoint ما زال متاحًا في الباك اند الحالي؟ إذا كان قد أُزيل أو استُبدل بمسار التوثيق عبر `POST /api/mobile/donations/{id}/proof` فقط، يرجى تأكيد ذلك حتى نزيل الزر من التطبيق ونعتمد رفع الإثبات فقط لتجنّب طلبات تفشل بخطأ 404.

---

### 7. التبرع المباشر لجمعية (`TargetType=Charity`)
- الوثيقة الجديدة تضيف `TargetType=Charity (3)` مع حقل `CharityId` للتبرع مباشرة لصندوق الجمعية العام دون حملة أو حالة. تم تفعيل هذا في تطبيق الموبايل (زر "تبرع مباشر لهذه الجمعية" في صفحة تفاصيل الجمعية) عبر `POST /api/mobile/donations` مباشرة (بدون Endpoint مختصر مثل `/charities/{id}/donate`).
- هل هذا الافتراض صحيح، أم يوجد Endpoint مختصر مشابه لـ `/campaigns/{id}/donate` و`/cases/{id}/donate` لهذه الحالة لم يُذكر في الوثيقة؟

---
*تم إعداد هذا الملف لتسهيل التواصل المباشر مع فريق الباك اند.*
