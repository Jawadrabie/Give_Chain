# طلب ميزة جديدة: استقبال "طلبات المساعدة" من التطبيق — GiveChain Mobile

## المشكلة أولاً بصراحة تامة

قسم **"هل تحتاج إلى مساعدة؟"** ظاهر وبارز في الصفحة الرئيسية للتطبيق، ويفتح استمارة كاملة (`RequestHelpScreen`) يملأ فيها المستخدم: نوع المساعدة، وصف الحالة، عدد أفراد الأسرة، الدخل الشهري، ويرفق ملفات فعلية من جهازه (صور، تقارير طبية...).

**لكن الاستمارة غير مربوطة بأي API — لا يوجد endpoint لها أصلاً.** عند الضغط على "إرسال الطلب"، الكود الحالي ينفّذ:

```dart
setState(() => _isSubmitting = true);
// Simulate network request
await Future.delayed(const Duration(seconds: 2));
setState(() { _isSubmitting = false; _isSuccess = true; });
```

أي: تأخير مصطنع لثانيتين، ثم شاشة "تم استلام طلبك بنجاح" مع رقم طلب **مُولَّد محلياً على الجهاز** (`REQ-${DateTime.now().millisecondsSinceEpoch}`) لا علاقة له بأي سجل حقيقي. **كل البيانات التي يدخلها المستخدم، بما فيها الملفات المرفقة، تُرمى فوراً ولا تصل لأي مكان.**

هذا خطر فعلي: شخص في ضائقة حقيقية يظن أن طلبه وصل لـ"فريق البحث الاجتماعي" (كما تقول رسالة النجاح)، بينما لا يوجد أي سجل به في أي مكان. نحتاج naming الميزة رسمياً وبناء الـ endpoint، أو — لحين ذلك — سنخفي القسم من الواجهة صراحة بدل تركه يعرض نجاحاً وهمياً.

---

## ما هو مبني بالفعل في الواجهة (حتى تُبنى الـ API عليه مباشرة دون إعادة تصميم)

### الحقول التي يجمعها التطبيق حالياً

| الحقل | النوع | إلزامي | ملاحظات |
|---|---|---|---|
| نوع المساعدة (Category) | اختيار واحد من قائمة ثابتة | نعم | القيم الحالية في الواجهة أدناه |
| وصف الحالة | نص متعدد الأسطر | نعم | "اكتب تفاصيل الوضع المادي أو الاجتماعي أو الصحي" |
| عدد أفراد الأسرة | رقم صحيح | لا (اختياري حالياً في الواجهة) | |
| الدخل الشهري | رقم عشري (ريال) | لا | |
| مرفقات | ملفات متعددة (صور/PDF عادة) | لا | صور، تقارير طبية، إلخ |
| إقرار المستخدم بصحة البيانات | checkbox، يجب تفعيله قبل الإرسال | نعم (على مستوى الواجهة) | لا يُرسل كحقل، هو تحقق محلي فقط |

### قائمة "نوع المساعدة" المعروضة حالياً للمستخدم (نص عربي + مُعرِّف محلي)

| المعرّف الحالي في كود الواجهة | التسمية المعروضة |
|---|---|
| `financial` | مالية |
| `medical` | طبية |
| `food` | غذائية |
| `housing` | سكن |
| `education` | تعليم |
| `other` | أخرى |

هذه معرّفات نصية محلية فقط وليست enum رسمياً — **إن أعطيتمونا enum رقمي رسمي بترتيب مختلف سنُطابق النصوص عليه فوراً**، لا داعي لإبقاء هذا الترتيب إن كان لديكم تصنيف مختلف أو أدق (مثلاً تصنيف مطابق لتصنيفات الحالات `CaseCategory` الموجودة أصلاً في النظام — قد يكون إعادة استخدامها أنسب من تصنيف منفصل، انظر السؤال 1 أدناه).

---

## الشكل المقترح للـ endpoint (بما يطابق أسلوبكم في بقية الـ API)

بناءً على نمط `POST /api/mobile/complaints` (multipart لأن فيه مرفقات، ومعرَّف بالكامل من التوكن دون حقل `userId`):

```http
POST /api/mobile/help-requests
Content-Type: multipart/form-data
Authorization: Bearer <token>
```

| الحقل المقترح | النوع | إلزامي |
|---|---|---|
| `Category` | int (enum) | نعم |
| `Description` | string | نعم |
| `FamilyMembersCount` | int? | لا |
| `MonthlyIncome` | decimal? | لا |
| `attachments` | files (متعددة) | لا |

**الاستجابة المقترحة** (بنفس نمط `ComplaintResponse` أو `BenefitRequestResponse`):

```jsonc
{
  "id": "…",
  "requestNumber": "…",      // رقم مرجعي يعرضه التطبيق للمستخدم — كما فعلتم مع complaintNumber
  "userId": "…",             // من التوكن
  "category": 1,
  "description": "…",
  "familyMembersCount": 4,
  "monthlyIncome": 1200.00,
  "status": 0,                // enum: انظر السؤال 3
  "createdAt": "…",
  "medias": [ { "id", "url", "mediaType", "isPrimary", "uploadedAt" } ]
}
```

---

## أسئلتنا لكم

### 1. هل هذه ميزة جديدة كلياً، أم امتداد لمفهوم موجود عندكم؟

قبل أن تبنوا جدولاً وendpoint جديدين بالكامل: هل "طلب المساعدة" هذا هو فعلياً بداية دورة حياة **الحالة الإنسانية (`Case`)** من جهة المستفيد نفسه (شخص عادي يطلب أن يُصنَّف كحالة تستحق الدعم)؟ إن كان الأمر كذلك، فقد يكون الأنسب أن يُنشئ هذا الـ endpoint سجلاً في نفس جدول `Case` بحالة أولية مثل "طلب قيد المراجعة من الإدارة" بدل جدول منفصل تماماً — يوفر عليكم ازدواجية، ويسمح لفريقكم الإداري بترقية الطلب مباشرة إلى حالة منشورة إن قُبل.

إن لم يكن هذا مناسباً لمنطق النظام لديكم، فلا مشكلة ببناء كيان منفصل — فقط أردنا طرح البديل الأبسط أولاً.

### 2. تصنيف "نوع المساعدة" — enum جديد أم إعادة استخدام `CaseCategory`؟

النظام لديه أصلاً `CaseCategory` (نراه في `/api/lookup/case-categories`). هل تصنيف طلب المساعدة يجب أن يكون نفسه، أم تصنيفاً مختلفاً خاصاً بطلبات الأفراد (القائمة الستة أعلاه)؟

### 3. ما قيم enum الحالة (`Status`) لطلب المساعدة؟

نحتاج القيم الرقمية بالضبط لعرض تسمية عربية مناسبة لكل حالة (بنفس أسلوبنا مع `DonationStatus`/`ComplaintStatus`) — تحديداً: هل هناك حالة "تمت الموافقة وتحوّل الطلب إلى حالة منشورة" يمكن أن نربط منها المستخدم بصفحة الحالة الناتجة؟

### 4. هل يحتاج المستخدم لتتبّع طلباته السابقة؟

هل نحتاج `GET /api/mobile/help-requests` (بنفس نمط `GET /api/mobile/complaints` أو `benefits/my`) ليرى المستخدم حالة طلبه لاحقاً؟ حالياً لا توجد أي شاشة "طلباتي للمساعدة" في التطبيق لأن لا داعي لها بدون بيانات حقيقية — سنضيفها إن أكّدتم وجود الـ endpoint.

### 5. الإشعارات

هل يصدر إشعار عبر `NotificationsController` الموجود عند تغيّر حالة طلب المساعدة (قبول/رفض/طلب معلومات إضافية)؟ التطبيق يدعم الإشعارات أصلاً فلا حاجة لعمل إضافي من جهتنا إن كان `ReferenceId` يشير لهذا الطلب.

### 6. هل `FamilyMembersCount` و`MonthlyIncome` مطلوبان فعلاً لأتمتة تقييم الأولوية؟

إن كانت هذه البيانات ستُستخدم يدوياً فقط من قبل "فريق البحث الاجتماعي" (كما تقول رسالة النجاح الحالية في الواجهة)، فلا داعي لأي منطق تحقق خاص من جانبكم غير الحفظ كما هي. لكن إن كانت ستُستخدم لاحقاً في تصنيف تلقائي للأولوية، أخبرونا لنجعلها إلزامية من جهة الواجهة بدل اختيارية.

---

## ماذا سنفعل من جهتنا حتى تصلنا الإجابة

سنعرض تنبيهاً صريحاً في الواجهة أن هذه الميزة "قيد التطوير" بدل شاشة النجاح الوهمية الحالية، حتى لا يظن أي مستخدم أن طلبه وصل فعلياً دون أن يكون كذلك.

---

*أُعدّ هذا الملف من فريق تطبيق الموبايل بتاريخ 2026-08-24.*
