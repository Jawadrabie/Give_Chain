# تطبيق تحديثات الباك اند بتاريخ 2026-08-30 — تقرير التنفيذ (الموبايل)

**التاريخ:** 2026-08-31
**المرجع:** `BACKEND_CHANGES_FOR_MOBILE.md` + `MOBILE_API_CHANGES.md`
**التحقّق:** تمّت مطابقة كل بند مع **Swagger الحيّ** (`/swagger/mobile/swagger.json`) ومع بيانات production فعلية.
**الحالة:** ✅ كل التعديلات مطبَّقة — `flutter analyze` نظيف و **85 اختباراً** ناجحاً.

---

## 1 — `caseNeeds` / `caseUpdates` (يُغلق العطل الحرج)

هذا البند يُغلق `BACKEND_BUG_REPORT_CASE_NEEDS.md`: **التبرع للحالات كان معطّلاً 100%** لأن الخادم يشترط `CaseNeedId` ولم يكن هناك أي مصدر لجلبه. الآن صار متاحاً.

**ملاحظة مهمة على الأسماء:** طلبنا في التقرير حقل `needs` بحقول `type` / `targetQuantity`، وأنتم أرسلتم `caseNeeds` بحقول `caseNeedType` / `quantity` / `amount`. **التطبيق يقبل الاثنين** (`caseNeeds` أولاً ثم `needs`، و`caseNeedType` ثم `type`) حتى لا يتعطّل مع أي من الشكلين.

ما تم في التطبيق:

- موديل `CaseNeed` و`CaseUpdate` مع `progress` محسوبة بالمقياس الصحيح: بالمبلغ إن كان `amount > 0`، وبالكمية إن كان `quantity > 0`.
- **شاشة التبرع:** القائمة المنسدلة صارت تُعبَّأ فعلياً، وتحتها بطاقة تفصيلية (المطلوب / المنجز / شريط تقدّم / الوصف). التحذير الأصفر «لا توجد احتياجات» لم يعد يظهر إلا إذا كانت الحالة فعلاً بلا احتياجات.
- **اقتراح نوع التبرع:** عند اختيار احتياج نقترح `donationType` **من شكل الاحتياج** (وجود `amount` = مالي، وجود `quantity` = عيني)، مع إبقاء الاختيار قابلاً للتعديل من المتبرّع.
- **شاشة تفاصيل الحالة:** قسم «الاحتياجات» بكل احتياج وتقدّمه، وقسم «آخر المستجدات» (مرتّب من الأحدث، مع معرض صور).
- احترمنا ملاحظتكم: الحقلان يُقرآن من `GET /api/mobile/cases/{id}` فقط، ولا نطلب تفاصيل إضافية في شاشات القوائم.

> 🔴 **تنبيه مهم — `CaseNeedType` ≠ `DonationType`:**
> Swagger يُظهر أن `CaseNeedType` قيمه **`[0,1,2,3]`** بينما `DonationType` قيمه **`[1,2,3]`** — إنّهما enum مختلفان، ولا تُصدِّر الوثيقة أسماء الأعضاء. وبيانات production تؤكّد الاختلاف:
>
> | الاحتياج الحيّ | `caseNeedType` | شكله الفعلي |
> |---|---|---|
> | «رسوم دراسية» / «إيجار شهري» | **2** | `amount` فقط → **مالي** |
> | «أدوية» (10 قطع) | **1** | `quantity` + `unit` → **عيني** |
> | «صيانة كرسي متحرك» | **3** | لا مبلغ ولا كمية → خدمة |
>
> لاحظوا أن `caseNeedType = 2` يعني **مالي**، بينما `DonationType = 2` يعني **عيني** — أي أن نسخ القيمة مباشرة كان سيختار النوع الخاطئ ويُرفض الطلب. لذلك **لا نستخدم `caseNeedType` إطلاقاً** لتحديد نوع التبرع أو لعرض تسمية للمستخدم، ونعتمد على `amount`/`quantity` بدلاً منه.
>
> **المطلوب منكم:** تصدير أسماء أعضاء `CaseNeedType` (مثلاً عبر `x-enumNames` في Swagger أو رسالة نصية)، خصوصاً معنى القيمة **`0`** التي لا مقابل لها في `DonationType`. عندها نستبدل الاستنتاج بمطابقة صريحة.

## 2 — `targetType` في الشكاوى

- `ComplaintTargetType`: `0 = Admin` (ضد GiveChain) و`1 = Charity` (الافتراضي).
- `CharityId` **يُحذف من الطلب كلياً** عند `targetType = 0` — إرسال نص فارغ كان سيفشل في ربط الـ `Guid`.
- `charityId` في الرد صار `String?` كما ذكرتم، **و** نعامل `Guid.Empty` معاملة `null` احترازياً.
- **الواجهة:** من صفحة الجمعية يظهر اختيار بين الجهتين؛ ومن «حسابي» أضفنا مدخلاً جديداً **«شكوى على المنصة»** (`/complaints/new`) — لم يكن هناك أي طريق للوصول لهذه الميزة قبل ذلك. قائمة الشكاوى وتفاصيلها تعرضان الجهة المشتكى عليها.

> 💡 **اقتراح صغير:** `ComplaintResponse` يحوي `charityId` لكن **بلا `charityName`**. لعرض اسم الجمعية في قائمة «شكاواي» نضطر حالياً لعرض كلمة «جمعية» فقط أو لجلب كل جمعية على حدة. إضافة `charityName` للردّ (كما فعلتم في نماذج أخرى) تُغنينا عن ذلك — غير عاجل.

## 3 — `charityId` في `benefit-types`

مطبَّق، مع معالجة `Guid.Empty` كقيمة غير موجودة. صار يُستخدم كقيمة احتياطية عند بناء مسار تقديم الطلب.

✅ **تحقّقنا من الإصلاح على الخادم الحيّ:** `GET /api/mobile/charities/{id}/benefit-types` صار يُرجع `charityId` الصحيح فعلاً (لم يعد `Guid.Empty`). ومع ذلك نعامل `Guid.Empty` معاملة القيمة الفارغة احترازياً.

> 🔎 **`fieldType` — سؤال مُغلق:** مثال `MOBILE_API_CHANGES.md` عرضه كنص (`"Numeric"`)، لكن **Swagger يُعرّفه `integer` بقيم `[0..6]`** وبيانات production تُرسل أرقاماً (`fieldType: 0`). إذاً الرقم هو العقد المعتمد والمثال النصّي كان توضيحياً فقط — **لا حاجة لأي إجراء من طرفكم**. التطبيق يقبل الشكلين تحسّباً لتفعيل string enum converter مستقبلاً.

## 4 — `phoneNumber`

مضاف بالكامل: التسجيل (`multipart`) وتسجيل الدخول (`AuthResponse`) وعرض/تعديل البروفايل، مع تحقّق مشترك من صيغة الرقم. الحقل اختياري بالكامل، والحسابات القديمة التي ترجع `null` تعمل طبيعياً.

- عند **مسح** رقم محفوظ نرسل `phoneNumber: null` صراحةً (وليس حذف المفتاح)، تماشياً مع سلوك `countryId`/`cityId` عندكم.
- **تذكير:** ذكرتم أن migration الـ `phoneNumber` قد لا يكون مطبَّقاً على قواعد بيانات Staging/Production. نرجو تأكيد تطبيقه، وإلا سيرجع الحقل `null` دائماً حتى بعد الحفظ.

---

## 5 — اختبار حيّ شامل لكل المسارات (End-to-End)

أنشأنا حساباً حقيقياً على production ونفّذنا كل تدفّقات التطبيق فعلياً. **النتيجة: كل الميزات تعمل.**

| التدفّق | النتيجة |
|---|---|
| تسجيل حساب + `PhoneNumber` | ✅ `200` — الرقم رجع في `AuthResponse` |
| تسجيل الدخول | ✅ `200` — الرقم المحدَّث رجع صحيحاً |
| تعديل البروفايل (`PUT`) ثم قراءته | ✅ الرقم حُفظ ورجع — **migration مطبَّق فعلاً** |
| **التبرع لحالة — احتياج مالي** | ✅ `200` «Donation pledged» |
| **التبرع لحالة — احتياج عيني** | ✅ `200` — العطل الحرج مُغلق نهائياً |
| شكوى على المنصة (`TargetType=0`) | ✅ `200` — `charityId: null` كما هو متوقَّع |
| قائمة الشكاوى / التفاصيل | ✅ `targetType` يرجع صحيحاً |
| أنواع الاستفادة + الأسئلة | ✅ `charityId` صحيح، `fieldType` رقمي |
| تقديم طلب استفادة | ⚠️ يُقبل `200` لكن الرد ناقص — راجع 5.1 |
| إجابات المعلومات (`PUT`/`GET`) | ✅ حفظ وقراءة سليمان |
| سجل التبرعات + الفلاتر | ✅ `200`، و`entityName` يعرض اسم الاحتياج |
| تتبّع التبرع (`trace`) | ✅ يعمل، ويشمل عقدة `type: 4` للاحتياج |
| الرئيسية / الحملات / الجمعيات / المراكز / الـ lookups | ✅ كلها `200` |
| الصور النسبية (`Images/Charities/…`) | ✅ `UrlResolver` يبنيها صحيحة (`200 image/jpeg`) |

> ℹ️ **الترميز العربي سليم:** ظهر لنا نصّ مشوّه (`?????`) في اختبار أوّلي، لكن تبيّن أنه من طرفية Windows لا من الـ API — عند إرسال بايتات UTF-8 صحيحة رجع النص عربياً سليماً. **لا مشكلة في التطبيق.**

### 5.1 🟠 عطل: `GET /api/mobile/benefits` يُرجع طلبات فارغة

الطلب يُنشأ بنجاح، لكن الرد (في الإنشاء **وفي القائمة**) يُفرِّغ أغلب الحقول:

```jsonc
{
  "id": "3ae77cbe-…",                                   // ✅ صحيح
  "requestNumber": "9d8ea6747d",                        // ✅ صحيح
  "status": 0,                                          // ✅ صحيح
  "benefitTypeId": "00000000-0000-0000-0000-000000000000",  // ❌ Guid.Empty
  "benefitTypeName": "",                                // ❌ فارغ
  "personId": "00000000-0000-0000-0000-000000000000",   // ❌ Guid.Empty
  "submittedAt": "0001-01-01T00:00:00",                 // ❌ DateTime.MinValue
  "answers": []                                         // ❌ فارغة رغم إرسال إجابات
}
```

**الأثر:** شاشة «طلباتي» كانت ستعرض بطاقة بلا اسم، وتاريخاً بلا معنى (`0001-01-01`)، وبلا أي إجابة.

**ما فعلناه مؤقتاً في التطبيق (حلّ عرضٍ فقط):**
- إخفاء التاريخ كلياً عند `0001-01-01` بدل عرضه.
- تخزين ما أرسله المستخدم فعلاً محلياً (اسم نوع الاستفادة + الجمعية + الإجابات) ودمجه في القائمة.
- **الأولوية دائماً لبيانات الخادم** — أي حقل ترسلونه صحيحاً يتجاهل النسخة المحلية فوراً، فيصبح هذا الحلّ بلا أثر تلقائياً بمجرد إصلاحكم.

**المطلوب:** تعبئة `benefitTypeId` و`benefitTypeName` و`personId` و`submittedAt` و`answers` في `BenefitRequestResponse`.

## 6 — ملاحظتان على التوثيق (اتفقنا أنهما غير عاجلتين)

1. **`/api/media` غير موثّق في أي Swagger** — يعمل فعلاً (`200`)، وسيُرفع لاحقاً حسب اتفاقنا.
2. **`/api/lookup/benefit-types` يرجع `404`** — التطبيق يرجع تلقائياً إلى `/api/mobile/charities/{id}/benefit-types`، وسيلتقط الـ lookup تلقائياً عند نشره.

---

## ملاحظة عامة

لم نطبّق شيئاً من `BACKEND_CHANGES_FOR_FRONTEND.md` — كل بنوده تخص لوحات الويب (`api/dashboard/*` و`api/center/*`) ولا تمسّ تطبيق الموبايل.

**ملخص ما ننتظره منكم:**

| # | الطلب | الأولوية |
|---|---|---|
| 1 | تعبئة حقول `BenefitRequestResponse` الفارغة (بند 5.1) | 🟠 مهم |
| 2 | أسماء أعضاء `CaseNeedType` (وخاصةً معنى `0`) | 🟠 مهم |
| 3 | إضافة `charityName` إلى `ComplaintResponse` | 🟡 تحسين |
| 4 | تصحيح `fieldType` لحقل «تاريخ الميلاد» من `1` (Numeric) إلى `3` (Date) — بيانات لا كود، وما زال كما هو | 🟡 تحسين |

**مُغلق:**
- ~~الشكل المعتمد لـ `fieldType`~~ — Swagger أكّد أنه `integer`.
- ~~تأكيد migration الـ `phoneNumber`~~ — تحقّقنا عملياً: **مطبَّق ويعمل**.

---

## 7 — إصلاحات إضافية في الواجهة اكتشفها الاختبار الحيّ

1. **خريطة تتبّع التبرعات كانت تعرض مسارات مغلوطة.** `GET /donations/trace` يُرجع **كل** التبرعات في قائمة أضلاع واحدة، وكانت الشاشة ترسمها كسلسلة واحدة متّصلة — فيظهر أن وجهة تبرّعٍ ما «تتدفّق» إلى متبرّع التبرع التالي. الآن تُقسَّم الأضلاع إلى رحلات منفصلة، ويفصل بينها فاصل بصري.
2. **بطاقات الاستفادة** لم تعد تعرض تاريخ `0001-01-01`.
3. **شاشة التبرع للحالة**: بطاقة تفصيلية للاحتياج المختار (المطلوب/المنجز/شريط تقدّم) + اقتراح نوع التبرع من شكل الاحتياج.
4. **تفاصيل الحالة**: قسما «الاحتياجات» و«آخر المستجدات».
5. **مدخل «شكوى على المنصة»** في «حسابي».

**التحقّق النهائي:** `flutter analyze` نظيف · **88 اختباراً** ناجحاً · `flutter build apk --debug` ✅ ينجح.
