# أسئلة الجولة الثانية لمسؤول الباك اند — GiveChain Mobile

شكراً على الإجابات التفصيلية في الجولة الأولى (`BACKEND_ANSWERS.md`) — كانت واضحة وحلّت معظم الغموض. تم تنفيذ ما يلي في التطبيق بناءً عليها:

- ✅ حذف `confirm-payment` بالكامل من التطبيق (سؤال 6).
- ✅ إجبار المستخدم على اختيار `CaseNeed` قبل التبرع لحالة، مع منع الإرسال نهائياً بدونه (سؤال 5).
- ✅ تأكيد أن `attachments` عند الإنشاء و`files` عند رفع الإثبات — كان التطبيق مطابقاً أصلاً (سؤال 2).
- ✅ التبرع المباشر للجمعية عبر `POST /api/mobile/donations` مع `TargetType=Charity(3)` + `CharityId` (سؤال 7).

بقي بندان مفتوحان من الجولة الأولى، بالإضافة إلى أسئلة جديدة ظهرت أثناء التنفيذ:

---

## أولاً: بنود مفتوحة من الجولة الأولى

### 1. بفيانات الحساب البنكي / IBAN — **الأهم، يحجب تجربة المستخدم**

هذه ليست مجرد نقطة تقنية، بل **فجوة فعلية في التدفق**: التطبيق حالياً يطلب من المتبرع أن "يحوّل بنكياً ثم يرفع إثبات التحويل"، لكنه **لا يستطيع أن يعرض له رقم حساب أو IBAN ليحوّل إليه** — لأن هذه البيانات غير موجودة في أي API.

عملياً المتبرع يرى: "حوّل ثم أرفق الإشعار" … دون أن يعرف **إلى أين يحوّل**.

نحتاج قراراً بين ثلاثة خيارات:

| الخيار | ما يلزم من الباك اند | ملاحظات |
|---|---|---|
| **أ. حقول على الجمعية** (مفضّل) | إضافة `BankName` / `BankAccountNumber` / `IBAN` / `AccountHolderName` إلى `CharityResponse` | يسمح لكل جمعية بحساب مستقل، وهو الأقرب لمنطق النظام |
| **ب. حساب موحّد للمنصة** | Endpoint واحد مثل `GET /api/mobile/platform/bank-accounts` | أبسط، لكن كل التحويلات تذهب لحساب المنصة |
| **ج. خارج النظام** | لا شيء | نعرض رقم حساب ثابت داخل التطبيق (hardcoded) — **غير مستحسن**: أي تغيير في الحساب يتطلب إصدار تحديث جديد للتطبيق على المتاجر |

**سؤالنا:** أي خيار تعتمدون؟ وإن كان (أ) أو (ب)، ما الجدول الزمني المتوقع؟ حتى ذلك الحين، هل نخفي خيار "تحويل بنكي" ونكتفي بـ"نقدي/تسليم في مركز"؟

---

### 2. Endpoint لتفاصيل تبرع واحد — نرجو إضافته

عرضتم إضافته عند الطلب، **ونعم نطلبه رسمياً**. السبب أن الحل المؤقت مكلف فعلياً: لعرض شاشة تفاصيل تبرع واحد، التطبيق يمسح سجل التبرعات صفحة صفحة (حتى 20 صفحة × 50 عنصر) بحثاً عن الـ `Id`.

هذا يعني أن فتح تبرع قديم قد يستهلك **عشرات الطلبات** ويفشل تماماً إن تجاوز التبرع حدّ المسح.

**المطلوب:** `GET /api/mobile/donations/{id}` يرجع `DonationResponse` واحداً (مقيّداً بـ `CurrentUserId` طبعاً).

> ملاحظة: لو تعذّر ذلك، البديل المقبول هو إضافة فلتر `Id` (أو `DonationIds`) إلى `DonationFilterRequest` الحالي، وهو أبسط بكثير على الأرجح.

---

## ثانياً: أسئلة جديدة

### 3. احتياجات الحالة (`CaseNeed`) — كيف نجلبها أصلاً؟

هذه **متلازمة مباشرة مع إجابتكم على السؤال 5**: أكّدتم أن `caseNeedId` **إلزامي** لكل تبرع لحالة ولا يوجد ربط تلقائي.

لكن وثيقة `API_MOBILE.md` نفسها تقول في قسم Cases:

> "a case's donatable *needs* (`CaseNeed`) aren't exposed as a distinct field on `CaseResponse` … check with the backend team if a needs-listing endpoint is required."

فنحن أمام تناقض: **الحقل إلزامي، لكن لا يوجد طريق موثّق لجلب قيمته.**

حالياً التطبيق يقرأ مصفوفة `needs` من داخل استجابة `GET /api/mobile/cases/{id}` إن وُجدت، وإن لم توجد فهو يمنع التبرع ويعرض رسالة "لا توجد احتياجات متاحة".

**أسئلتنا:**
- هل `CaseResponse` يتضمن فعلياً مصفوفة `needs` (حتى لو لم تُوثَّق)؟ وإن نعم، ما اسم الحقل بالضبط وما شكل كل عنصر؟ نحتاج على الأقل: `Id`، الاسم/العنوان، `CaseNeedType` (مال/مادة/خدمة)، `Unit`، الكمية المطلوبة والمُنجزة.
- إن لم يكن مضمّناً، هل يمكن إضافة `GET /api/mobile/cases/{id}/needs`؟
- **مهم للتحقق من جهة التطبيق:** ذكرتم أن نوع التبرع يجب أن يطابق `CaseNeedType`. لكي نمنع المستخدم من اختيار تركيبة خاطئة قبل الإرسال، نحتاج قيمة `CaseNeedType` ومقابلها الرقمي. ما هي قيم هذا الـ enum بالضبط؟ وهل تطابق `DonationType` (`Money=1, Item=2, Service=3`) أم لها ترقيم مختلف؟

---

### 4. `EntityId` / `EntityName` في حالة `TargetType=Charity`

جدول `DonationResponse` في الوثيقة يعرّف:

> `EntityId` — Campaign.Id when TargetType=Campaign, or CaseNeed.Id when Case

ولا يذكر ماذا يحدث عندما يكون `TargetType=Charity (3)` (وهو النوع الجديد الذي نفّذناه).

**سؤالنا:** في التبرع المباشر للجمعية، ماذا يحتوي `EntityId` و`EntityName`؟ هل `EntityId = CharityId` و`EntityName = اسم الجمعية`؟ أم يعودان فارغين/`Guid.Empty`؟

نحتاج الجواب لأن التطبيق يعرض `EntityName` كعنوان لكل صف في سجل التبرعات — وإن كان فارغاً سنحتاج fallback مناسب.

---

### 5. فلاتر `GET /api/mobile/donations` — تأكيد السلوك

الوثيقة تسرد فلاتر كثيرة (`Search`, `FromDate`/`ToDate`, `MinAmount`/`MaxAmount`, `DonationType`, `PaymentMethod` …). قبل أن نبني عليها واجهة تصفية كاملة:

- **`Search`**: على أي حقول يبحث بالضبط؟ الوثيقة تقول (message / item name / service description) — هل يشمل أيضاً اسم الحملة/الحالة (`EntityName`)؟ المتبرع غالباً سيبحث باسم الحملة لا بنص رسالته.
- **`FromDate`/`ToDate`**: هل تُقارن بتوقيت UTC؟ التطبيق يرسل ISO-8601 بصيغة UTC — نرجو التأكيد حتى لا تختلف حدود اليوم.
- **`ThroughCenter=false`**: هل تعني "المباشرة فقط" (أي `CenterId == null`)، أم تُتجاهل تماماً كما لو لم تُرسل؟
- هل الفلاتر تعمل مجتمعة بمنطق **AND**؟

---

### 6. أنواع الحقول الديناميكية (`FieldType`) — التعامل مع `Attachment` و`MultiOption`

في `PersonInfoField` و`BenefitTypeQuestion`، الـ `FieldType` يشمل: `Text, Numeric, Boolean, Date, Attachment, Option, MultiOption`.

الإجابة تُرسل دائماً كـ `Answer` من نوع `string?`. أسئلتنا:

- **`Attachment`**: كيف نرسل ملفاً؟ لا يوجد `multipart` في `PUT /api/mobile/profile/info-answers` ولا في `POST /api/mobile/benefits`. هل نرفع الملف أولاً عبر `POST /api/media` ثم نضع الـ URL الناتج نصاً في `Answer`؟ وإن نعم، ما قيمة `ownerType` المطلوبة عند الرفع؟
- **`MultiOption`**: كيف نرسل اختيارات متعددة داخل حقل `Answer` النصي الواحد؟ مفصولة بفاصلة؟ أم كمصفوفة JSON مُسلسلة نصاً؟ نحتاج الصيغة المتوقعة بالضبط لأن أي تخمين سيُخزَّن خطأً.
- **`Boolean`**: `"true"`/`"false"` أم `"1"`/`"0"`؟
- **`Date`**: ما الصيغة؟ `yyyy-MM-dd` أم ISO-8601 كامل؟

> ملاحظة: `PersonInfoAnswerResponse` يحتوي حقل `Attachments` (مصفوفة روابط) بجانب `Answer` — مما يوحي بأن للمرفقات مساراً مختلفاً عن `Answer`. نرجو توضيح كيف تُملأ هذه المصفوفة.

**نقطة إضافية مهمة:** `PersonInfoFieldResponse` (من `GET /api/mobile/auth/info-fields`) يحتوي فقط `Id, FieldName, FieldType, UserType, IsRequired` — **بدون حقل `Options`**، رغم أن `FieldType` يشمل `Option` و`MultiOption`. أي أنه إن أعددتم حقلاً من نوع `Option` فلا يستطيع التطبيق معرفة الخيارات المسموحة فيعرضه كنص حر، وسيُدخل المستخدم قيماً لن تطابق ما تتوقعونه.

بالمقابل `BenefitTypeQuestionResponse` **يحتوي** `Options` بشكل صحيح.

**المطلوب:** إضافة `Options` (مصفوفة نصوص) إلى `PersonInfoFieldResponse` أسوةً بـ`BenefitTypeQuestionResponse`.

---

### 9. `reset-password`: هل الحقل `token` أم `code`؟ — **يحجب استعادة كلمة المرور**

تناقض مباشر بين وثيقتين لديكم:

- `API_MOBILE.md` (الأحدث): `ResetPasswordRequest`: `Email`, **`Token`**, `NewPassword`
- `GiveChain_Mobile_Guide.md` (الأقدم): `{ "email": …, "code": "123456", "newPassword": … }` — وهي نفسها تُدرج هذا كسؤال مفتوح في قائمة استفساراتها: *"confirm exact `ResetPasswordRequest` field names (`code` vs `token`)"*

التطبيق كان يرسل `code` فقط، ما يعني أن **استعادة كلمة المرور غالباً معطّلة تماماً** إن كان العقد الفعلي `Token`.

**ما فعلناه مؤقتاً:** التطبيق الآن يرسل **الحقلين معاً** (`token` و`code` بنفس القيمة) — لأن ASP.NET يتجاهل أي حقل غير معرَّف في الـ DTO، فيعمل التدفق تحت أي من العقدين دون مخاطرة.

**المطلوب منكم:** تأكيد الاسم الصحيح لنحذف الزائد. وهل القيمة رمز رقمي قصير (OTP) أم توكن طويل؟ وما مدة صلاحيته؟

---

### 7. تفعيل `[Authorize]` — متى؟

الوثيقة تذكر أن `[Authorize]` معطّل (مُعلَّق) على كل الكونترولرات عدا `NotificationsController`، مع نصيحة أن نتعامل مع كل شيء كأنه يتطلب توكن.

التطبيق فعلاً يرسل `Bearer` على كل الطلبات غير العامة. سؤالنا فقط للتخطيط:

- متى يُتوقّع تفعيله فعلياً؟ نريد التأكد أن أي endpoint نعتبره اليوم "عاماً" (تصفح الحملات/الحالات/الجمعيات دون تسجيل دخول) **سيبقى** `AllowAnonymous` بعد التفعيل ولن ينكسر تصفح الزائر.
- تحديداً: هل `GET /api/mobile/home`، `charities`، `campaigns`، `cases` وتفاصيلها ستبقى متاحة بدون توكن؟

---

### 8. `MediaFileResponse` — تأكيد الحقول

الوثيقة تشير إلى `Medias` كـ `MediaFileResponse[]` دون جدول حقول صريح، والمثال يُظهر: `id`, `url`, `mediaType`, `isPrimary`, `uploadedAt`.

- هل هذه هي كل الحقول؟
- ما قيم `mediaType` الرقمية ومعانيها؟ نحتاجها للتمييز بين صورة وملف PDF حتى لا يحاول التطبيق عرض PDF كصورة في معرض الصور.

---

## ثالثاً: ملاحظة صغيرة عن ترميز الملف

وصل ملف الإجابات السابق بترميز مُشوَّه (تظهر العربية كرموز مثل `Ø¥Ø¬Ø§Ø¨Ø§Øª`) — يبدو أنه حُفظ بترميز غير UTF-8. أعدنا كتابته يدوياً بترميز سليم. نرجو حفظ الملفات القادمة بـ **UTF-8** لتفادي أي سوء قراءة.

---

*أُعدّ هذا الملف من فريق تطبيق الموبايل بتاريخ 2026-08-23، بعد تنفيذ إجابات الجولة الأولى فعلياً في الكود.*
